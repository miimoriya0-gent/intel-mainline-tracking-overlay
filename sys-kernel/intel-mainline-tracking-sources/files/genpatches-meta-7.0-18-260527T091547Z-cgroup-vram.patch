diff --git a/1009_linux-7.0.10.patch b/1009_linux-7.0.10.patch
index dff61c71..8325de84 100644
--- a/1009_linux-7.0.10.patch
+++ b/1009_linux-7.0.10.patch
@@ -9373,15 +9373,6 @@ diff --git a/drivers/gpu/drm/ttm/ttm_bo.c b/drivers/gpu/drm/ttm/ttm_bo.c
 index 0765d69423d2f4..08d07ffbb1674a 100644
 --- a/drivers/gpu/drm/ttm/ttm_bo.c
 +++ b/drivers/gpu/drm/ttm/ttm_bo.c
-@@ -740,7 +740,7 @@ static int ttm_bo_alloc_resource(struct ttm_buffer_object *bo,
- 		may_evict = (force_space && place->mem_type != TTM_PL_SYSTEM);
- 		ret = ttm_resource_alloc(bo, place, res, force_space ? &limit_pool : NULL);
- 		if (ret) {
--			if (ret != -ENOSPC && ret != -EAGAIN) {
-+			if (ret != -ENOSPC) {
- 				dmem_cgroup_pool_state_put(limit_pool);
- 				return ret;
- 			}
 @@ -1178,17 +1178,13 @@ ttm_bo_swapout_cb(struct ttm_lru_walk *walk, struct ttm_buffer_object *bo)
  		bdev->funcs->swap_notify(bo);
  
@@ -9457,19 +9448,6 @@ index 192fca24f37e44..bfd9c68fcd9c1f 100644
  /* Move a resource to the LRU or bulk tail */
  void ttm_resource_move_to_lru_tail(struct ttm_resource *res)
  {
-@@ -385,8 +398,11 @@ int ttm_resource_alloc(struct ttm_buffer_object *bo,
- 
- 	if (man->cg) {
- 		ret = dmem_cgroup_try_charge(man->cg, bo->base.size, &pool, ret_limit_pool);
--		if (ret)
-+		if (ret) {
-+			if (ret == -EAGAIN)
-+				ret = -ENOSPC;
- 			return ret;
-+		}
- 	}
- 
- 	ret = man->func->alloc(man, bo, place, res_ptr);
 diff --git a/drivers/gpu/drm/v3d/v3d_drv.c b/drivers/gpu/drm/v3d/v3d_drv.c
 index dd60acdf52c2b2..86e05fcf6cf656 100644
 --- a/drivers/gpu/drm/v3d/v3d_drv.c
